# PostgreSQL connection string
# Supabase: Settings → Database → Connection string → URI
# Local:    postgresql://postgres:password@localhost:5432/mik_platform
DATABASE_URL=

PORT=19080

# Telegram bot for consultation notifications (used via n8n)
TG_BOT_TOKEN=
TG_CHAT_ID=
